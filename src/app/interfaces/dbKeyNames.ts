/**
 * This interface will help us to keep the index db key
 * @author Jagat Bandhu
 * @since 1.0.0
 */
export interface IDBKeyNames {
    studentData: string;
    studentAttendanceData: string;
    userId: string
}

