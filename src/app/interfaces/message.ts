/**
 * This interface will help us to keep the waring and success messages over the project
 * @author Jagat Bandhu
 * @since 1.0.0
 */
interface IMessages {
    checkInternetConnection: string,
    serverError: string,
    networkError: string,
    pleaseWait: string, 
    validUserName: string,
    validPassword: string,
}
